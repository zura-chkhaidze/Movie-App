const home = "/";
const signin = "/sign-in";
const signup = "/sign-up";
const contact = "/contact";
const movies = "/movies";

const routes = {
  home,
  signin,
  signup,
  contact,
  movies,
};
export default routes;
